#include "common.h"
#include "wall.h"

GLuint firstSceneWallTextureNew;

void initWall(void)
{
    // firstSceneWallTextureNew = createTexture2D("");
}
void drawWall(void)
{
    /*
    // Main Wall
    glBegin(GL_QUADS);
    glVeretx3f(10.0f, 10.0f, 0.0f);
    glVertex3f(-10.0f, 10.0f, 0.0f);
    glVertex3f(-10.0f, -10.0f, 0.0f);
    glVertex3f(10.0f, -10.0f, 0.0f);
    glEnd();
*/
}
void uninitWall(void)
{
}
